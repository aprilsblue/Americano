if(<%= @is_present %> == true) {
  $('ul.friends_list').html(
    "<%= escape_javascript (render partial: 'my_pages/list.html.erb', locals: { followers: @followers, followees: @followees }) %>"
  );
}
$('input#email').val("<%= @message %>");
